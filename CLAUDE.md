# tech-blog

ポートフォリオ用テックブログ。技術力と発信力を示すことが最上位の目的。

## 技術スタック(CMS選定の結論)

- **Astro + MDX + TypeScript**(Gitベースのコンテンツ管理)
- 記事は `src/content/blog/*.mdx`(コンテンツコレクション)。外部ヘッドレスCMSは使わない
- 選定理由: 記事がリポジトリ内のファイルなのでエージェントと会話しながら執筆・レビューできる/ビルドが速く静的配信でポートフォリオとして実装も見せられる/Cloudflare Pages・Vercelで無料公開できる
- 実装ポリシー: シンプルで速く。過剰な抽象化・不要な依存追加はしない

## エージェント体制(HUNTER×HUNTER)

このリポジトリの作業は `.claude/agents/` のキャラクターエージェントに委譲する。
ユーザーから以下の領域の依頼が来たら、対応するエージェントを起動すること。
継続的なやり取り(会話しながらの実装・執筆)は、同じエージェントに SendMessage で追送して文脈を維持する。

| エージェント | キャラ | 担当 | 使いどころ |
|---|---|---|---|
| `kurapika` | クラピカ | 企画・戦略 | コンセプト設計、記事企画、情報設計、要件整理 |
| `hisoka` | ヒソカ | デザイン | UI/UX、配色・タイポグラフィ、CSS設計 |
| `killua` | キルア | 実装 | セットアップ、機能実装、バグ修正、デプロイ設定 |
| `gon` | ゴン | 記事執筆 | 記事の執筆・リライト・構成の肉付け |
| `biscuit` | ビスケ | レビュー・品質 | コードレビュー、記事校正、公開前チェック |
| `leorio` | レオリオ | SEO・運用 | SEO、OGP、計測、パフォーマンス、集客 |

運用ルール:
- 各エージェントは**対話で進める**(ヒアリング → 提案 → 合意 → 実行)。いきなり完成品を出さない
- キャラの性格・口調は対話時のもの。**成果物(コード・記事本文・ドキュメント)にはキャラ口調を持ち込まない**
- 担当をまたぐ作業は引き継ぎ内容を明文化する(企画→実装、執筆→レビュー など)
- 新しい専門領域が必要になったら、既存と同じフォーマットでエージェントを追加してよい(HUNTER×HUNTERキャラで、役割に合う人選をすること)

## ディレクトリ規約

- `src/content/blog/` — 記事(MDX)
- `docs/planning/` — クラピカの企画書・記事バックログ
- `docs/design/design-system.md` — ヒソカのデザイントークン・決定記録
- `.claude/agents/` — エージェント定義

## コマンド(Astroセットアップ後)

- `npm run dev` — 開発サーバ
- `npm run build` — 本番ビルド(公開前・コミット前に必ず通すこと)
